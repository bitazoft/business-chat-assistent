# To install:  pip install -r requirements.txt
#
# Versions are pinned with ~= (compatible release) so a patch fix is picked up
# but a breaking major/minor is not. Previously nothing was pinned at all, so
# two installs a week apart could get different major versions of LangChain -
# which is exactly the kind of thing that turns into a mystery outage.
#
# Vector search (sentence-transformers, faiss-cpu) is optional: it pulls in
# torch, ~2.5GB. The app runs without it as long as RAG_ENABLED=false.
# See requirements-ml.txt.

# ---- Web framework ----
fastapi~=0.141.1
uvicorn[standard]~=0.52.4
pydantic~=2.13.5
typing_extensions~=4.16.0

# ---- LLM ----
langchain~=1.3.18
langchain-core~=1.6.1
langchain-classic~=1.0.8
langchain-openai~=1.6.0
langchain-community~=0.4.2
tiktoken~=0.14.0

# Only needed when AI_PROVIDER=DEEPSEEK; agent.py imports it lazily.
# langchain-deepseek~=1.0.0

# ---- Database ----
sqlalchemy~=2.0.52
psycopg2-binary~=2.9.12

# ---- HTTP / utilities ----
requests~=2.34.2
python-dotenv~=1.2.3
numpy~=2.5.2

# ---- AWS S3 (only used when STORAGE_BACKEND=s3) ----
boto3~=1.43.83
botocore~=1.43.83

# ---- Data tooling (used by data/process_bitext_dataset.py) ----
pandas~=3.0.5
